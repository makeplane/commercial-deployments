apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: plane-live-secrets
  namespace: plane
stringData:
  LIVE_SERVER_SECRET_KEY: "htbqvBJAgpm9bzvf3r4urJer0ENReatceh"
  REDIS_URL: ""  # Will be filled by redis component
  AMQP_URL: "" # Will be filled by rabbitmq component

  # AWS Secret ARNs (optional - for managed ElastiCache / AmazonMQ).
  # With AMAZONMQ_SECRET_ARN set, AMQP_URL may be a bare host:port and live
  # fetches the broker credentials from Secrets Manager at startup.
  ELASTICACHE_SECRET_ARN: ""
  AMAZONMQ_SECRET_ARN: ""
