apiVersion: apps/v1
kind: Deployment
metadata:
  name: plane-space-wl
  namespace: plane
  labels:
    app.kubernetes.io/name: plane-enterprise
    app.kubernetes.io/component: space
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: plane-enterprise
      app.kubernetes.io/component: space
  template:
    metadata:
      labels:
        app.kubernetes.io/name: plane-enterprise
        app.kubernetes.io/component: space
    spec:
      serviceAccountName: plane-srv-account
      containers:
        - name: space
          image: makeplane/space-commercial:v2.3.3
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: plane-app-vars
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 1000Mi
              cpu: 500m
