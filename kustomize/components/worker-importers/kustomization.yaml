apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - deployment.yaml

patches:
  # Point silo at the dedicated importer queue. base defaults it to "celery" so
  # that base works without this component.
  - path: patches/silo-importers-queue.yaml
