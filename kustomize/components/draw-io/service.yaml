apiVersion: v1
kind: Service
metadata:
  name: plane-draw-io
  namespace: plane
  labels:
    app.kubernetes.io/name: plane-enterprise
    app.kubernetes.io/component: draw-io
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: plane-enterprise
    app.kubernetes.io/component: draw-io
  ports:
    - name: draw-io-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
