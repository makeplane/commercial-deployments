apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: plane-mcp-server-secrets
  namespace: plane
stringData:
  PLANE_OAUTH_PROVIDER_CLIENT_ID: ""     # replaced from overlay-secret-vars MCP_OAUTH_CLIENT_ID
  PLANE_OAUTH_PROVIDER_CLIENT_SECRET: "" # replaced from overlay-secret-vars MCP_OAUTH_CLIENT_SECRET
