apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: plane-mcp-server-secrets
  namespace: plane
stringData:
  PLANE_OAUTH_PROVIDER_CLIENT_ID: ""     # replaced from overlay-secret-vars MCP_OAUTH_CLIENT_ID
  PLANE_OAUTH_PROVIDER_CLIENT_SECRET: "" # replaced from overlay-secret-vars MCP_OAUTH_CLIENT_SECRET

  # ElastiCache endpoint + AWS Secrets Manager auth-token ARN (server.py:36)
  REDIS_HOST: ""             # replaced from overlay-secret-vars REDIS_HOST
  ELASTICACHE_SECRET_ARN: "" # replaced from overlay-secret-vars ELASTICACHE_SECRET_ARN
