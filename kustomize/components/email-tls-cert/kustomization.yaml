apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - issuer.yaml
  - certificate.yaml

patches:
  # Switch the tls-cert volume from emptyDir to the cert-manager-issued secret.
  - path: patches/use-tls-secret.yaml
