apiVersion: v1
kind: Service
metadata:
  name: plane-external-api
  namespace: plane
  labels:
    app.kubernetes.io/name: plane-enterprise
    app.kubernetes.io/component: external-api
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: plane-enterprise
    app.kubernetes.io/component: external-api
  ports:
    - name: external-api-8000
      port: 8000
      protocol: TCP
      targetPort: 8000
