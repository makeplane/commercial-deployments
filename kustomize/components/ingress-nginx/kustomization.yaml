apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# This component only patches the plane Ingress with nginx annotations.
# Install the ingress-nginx controller yourself, then set INGRESS_CLASS: "nginx" in vars.yaml.
patches:
  - path: patches/ingress-annotations.yaml
